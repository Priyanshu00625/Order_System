spring:
  application:
    name: USER-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://rathipreet28:Preet2525@ordersystem.helnuuf.mongodb.net/OrderSystem?appName=orderSystem
      auto-index-creation: true

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

server:
  port: 8081