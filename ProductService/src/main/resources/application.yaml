spring:
  application:
    name: PRODUCT-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://rathipreet28:Preet2525@ordersystem.helnuuf.mongodb.net/Products?appName=orderSystem
      auto-index-creation: true


server:
  port: 8084

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
